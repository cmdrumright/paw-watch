info:
  name: Delete comment forbidden
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base-url}}/api/comments/{{createdCommentId}}"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken2}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests: |
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
