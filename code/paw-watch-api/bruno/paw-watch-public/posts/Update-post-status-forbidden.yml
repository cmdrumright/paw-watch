info:
  name: Update post status forbidden
  type: http
  seq: 12

http:
  method: PATCH
  url: "{{base-url}}/api/posts/{{createdPostId}}/status"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken2}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {"status": "closed"}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests: |
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
