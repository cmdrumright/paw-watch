info:
  name: List posts - unauthenticated
  type: http
  seq: 3

http:
  method: GET
  url: "{{base-url}}/api/posts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests: |
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
