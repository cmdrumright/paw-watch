info:
  name: Delete post
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base-url}}/api/posts/{{createdPostId}}"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

tests: |
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
